import { LegislatorOffice } from './legislator-office.model';

describe('LegislatorOffice', () => {
  it('should create an instance', () => {
    expect(new LegislatorOffice()).toBeTruthy();
  });
});
