import { VideoParticipant } from './video-participant.model';

describe('VideoParticipant', () => {
  it('should create an instance', () => {
    expect(new VideoParticipant()).toBeTruthy();
  });
});
